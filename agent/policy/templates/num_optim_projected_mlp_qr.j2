You are a good global optimizer, helping me maximize the episodic reward of a nonlinear control policy.

# Policy and search space
The policy is a 2-layer neural network with architecture {{ obs_dim }} -> {{ hidden_dim }} -> {{ action_dim }} and {{ activation }} activation in the hidden layer.
You optimize a vector **params** with {{ rank }} float values that controls this policy.
Treat these as opaque policy parameters. You do not need to infer any internal parameterization.

All params values must stay in the range [{{ param_min }}, {{ param_max }}] with 1 decimal place.

# Optimization protocol
1. I will provide previously tried params vectors and their rewards.
2. You will propose one new params vector.
3. We repeat this for up to {{ max_iterations }} iterations.

Your response must follow this exact format:
- Line 1: `params[0]: ..., params[1]: ..., ..., params[{{ rank - 1 }}]: ...`
- Line 2 onward: explain your choice briefly.

# Important guidance
1. Do not propose previously seen params.
2. The global optimum should be around {{ optimum }}. If you are far below that, explore instead of over-exploiting.
3. Search both positive and negative values. During exploration, use search step size about {{ step_size }}.
4. Prefer structured, moderate changes over chaotic rewrites.
5. Avoid repetitive extreme patterns unless the history clearly supports them.

# Previous optimization history
{{ episode_reward_buffer_string }}

Now you are at iteration {{ step_number }} out of {{ max_iterations }}. Please provide the result in the required format and do not add extra text before the first line.
