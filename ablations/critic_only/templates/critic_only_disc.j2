You are an optimization agent, tasked to optimize a tabular policy for a reinforcement learning task in a single pass.

# Task description
{% if env_description %}{% include env_description %}{% endif %}

# What is a policy here
The policy is an array of {{ rank }} integers. Each entry params[i] says which action the agent takes in state i. The possible actions are {{ actions[0] }}.

# Previous optimization history
For reference, here are the policies that have been tried before and their rewards:
{{ history_json }}

# What actually happened during the latest test
Step-by-step trace from one recent evaluation episode. Each line shows the observed state, the action taken, and the reward at that step.

{{ trajectory_summary }}

# Your task
You must do all of the following internally before writing the final parameters:
1. Infer which regions of the state space are likely failing based on the task description, previous attempts, and the trajectory.
2. Draft an initial candidate policy.
3. Critique that initial candidate yourself, looking for brittle or obviously bad state-action choices.
4. Revise it once and output only the final revised policy.

Important points of consideration:
- Use the previous optimization history to avoid repeating failed ideas.
- Use the trajectory to identify concrete failure states or brittle decisions.
- Keep changes coherent across related states rather than making random isolated edits.
- Remember that each parameter value must be chosen from {{ actions[0] }}.
- Do not propose parameters that already appear in the previous attempts above.
- You may draft and critique internally, but do not print the draft policy.
- If your draft and final revision differ, output only the final revised policy.
{% if optimum %}- The target reward for this task is around {{ optimum }}.{% endif %}

Your response must have exactly two parts, in this order:

FIRST LINE (parameters only, nothing else before this):
params[0]: <value>, params[1]: <value>, ..., params[{{ rank - 1 }}]: <value>

This first line must contain only the final revised policy, not the draft.

REMAINING LINES (your reasoning):
Briefly explain the draft, the self-critique, and the final revision.
