You are an optimization agent, tasked to optimize a linear policy for a reinforcement learning control task. An initial set of parameters was just proposed and tested. Your job is to review, reflect on the test results and improve the policy parameters.

# Task description
{% if env_description %}{% include env_description %}{% endif %}

# Policy structure
The policy is a linear function with {{ rank }} floating-point parameters.
params values should be in the range [-6.0, 6.0] with 1 decimal place.
The episodic reward measures how well the policy performs.

# What was proposed
The optimizer suggested these parameters:
{{ proposed_params }}

# How it performed
Average reward over evaluation rollouts: {{ achieved_reward }}
{% if optimum %}Target reward for this task: around {{ optimum }}.{% endif %}

# What actually happened during the test
Below are three representative rollouts from the evaluation episodes: the worst one, one near the median, and the best one.

{{ trajectory_summary }}

# Previous attempts
For context, here are previous parameter settings and outcomes in a compact format.
Important:
- `Warmup trial` entries come from the random warmup phase and show a single rollout with `rollout_reward`.
- `Training trial` entries come from the optimization loop and show `mean_reward`, which is the average over evaluation rollouts for that trial.
- For `Training trial` entries, any displayed representative rollout was selected as the rollout whose reward is closest to the trial mean. It is one example rollout, not a rollout with reward equal to `mean_reward`.
{{ history_text }}

# Your task
Follow these steps before writing any parameters:
1. Compare the worst, median, and best rollouts to separate systematic failures from occasional ones.
2. Identify which state dimensions and action responses are associated with the poor rollouts.
3. Use the successful parts of the median/best rollouts as evidence for what to preserve.
4. Revise only the parameters most responsible for instability or underperformance.

Important points of consideration:
- If poor behavior appears in both the worst and median rollouts, it is likely a real policy problem.
- If it appears only in the worst rollout, be careful not to overfit to a rare stochastic failure.
- Use the best rollout as evidence for parameters or behaviors worth keeping.
- Keep changes focused.
- Prefer adjustments of {{ step_size }} or less per parameter unless the evidence strongly supports a larger change.
- Do not propose parameters that already appear in the previous attempts above.

Your response must have exactly two parts, in this order:

FIRST LINE (parameters only, nothing else before this):
params[0]: <value>, params[1]: <value>, ..., params[{{ rank - 1 }}]: <value>

REMAINING LINES (your reasoning):
Explain what you changed and why, referencing specific rollouts and states from the compact summaries.
