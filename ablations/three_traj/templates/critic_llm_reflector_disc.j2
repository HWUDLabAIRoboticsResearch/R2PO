You are an optimization agent, tasked to optimize a tabular policy for a reinforcement learning task. An initial set of parameters was just proposed and tested. Your job is to review, reflect on the test results and improve the policy parameters.

# Task description
{% if env_description %}{% include env_description %}{% endif %}

# What is a policy here
The policy is an array of {{ rank }} integers. Each entry params[i] says which action the agent takes in state i. The possible actions are {{ actions[0] }}.

# What was proposed
The optimizer suggested the following policy:
{{ proposed_params }}

# How it performed
The policy was evaluated in the environment and achieved an average reward of {{ achieved_reward }}.
{% if optimum %}The target reward for this task is around {{ optimum }}.{% endif %}

# What actually happened during the test
Below are three representative rollouts from the 20 evaluation episodes: the worst one, one near the median, and the best one.

{{ trajectory_summary }}

# Previous optimization history
For reference, here are previously tried policies and their outcomes in a compact format.
Important:
- `Warmup trial` entries come from the random warmup phase and show a single rollout with `rollout_reward`.
- `Training trial` entries come from the optimization loop and show `mean_reward`, which is the average over evaluation rollouts for that trial.
- For `Training trial` entries, any displayed representative rollout was selected as the rollout whose reward is closest to the trial mean. It is one example rollout, not a rollout with reward equal to `mean_reward`.
{{ history_text }}

# Your task
Follow these steps before writing any parameters:
1. Compare the worst, median, and best rollouts to identify which failures are consistent versus occasional.
2. Locate the state-action decisions in the policy that most likely explain the bad rollouts.
3. Preserve action choices that appear in the successful parts of the median/best rollouts.
4. Revise only the parameters most responsible for the failures or missed opportunities.

Important points of consideration:
- If the same failure appears in the worst and median rollouts, treat it as a systematic issue.
- If a failure appears only in the worst rollout, consider whether it may be due to stochasticity and avoid over-correcting.
- Use the best rollout as evidence for what is already working and should be preserved.
- Keep changes focused. If part of the policy is working, preserve those parameters.
- Remember that each parameter value must be chosen from {{ actions[0] }}.
- Do not propose parameters that already appear in the previous attempts above.

Your response must have exactly two parts, in this order:

FIRST LINE (parameters only, nothing else before this):
params[0]: <value>, params[1]: <value>, ..., params[{{ rank - 1 }}]: <value>

REMAINING LINES (your reasoning):
Explain what you changed and why, referencing specific rollouts and states from the compact summaries.
